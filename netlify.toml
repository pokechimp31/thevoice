# netlify.toml
# Tells Netlify where your serverless functions live
# and enables Identity for admin login.

[build]
  functions = "netlify/functions"
  publish   = "."

# Redirect /api/* to your serverless functions
[[redirects]]
  from   = "/api/*"
  to     = "/.netlify/functions/:splat"
  status = 200
